# 🔧 Исправление проблемы с Firebase в Unity

## Проблема
Unity не может подключиться к Firebase и показывает ошибку:
```
[SubscriptionManager] Exception loading subscription settings: Failed to get FirebaseDatabase instance
```

## ✅ Решение

### 1. Обновленные файлы
- `scripts/SubscriptionManager.cs` - добавлена правильная инициализация Firebase
- `FIREBASE_UNITY_SETUP.md` - подробная инструкция по настройке

### 2. Что нужно сделать в Unity:

#### Шаг 1: Проверьте Firebase SDK
1. Откройте Unity Editor
2. `Window > Package Manager`
3. Убедитесь, что установлены:
   - **Firebase Database**
   - **Firebase Core**

#### Шаг 2: Проверьте google-services.json
1. Файл должен быть в корне папки `Assets`
2. Содержит правильные данные проекта

#### Шаг 3: Настройте Build Settings
1. `File > Build Settings`
2. Платформа: **Android**
3. `Edit > Project Settings > Player`
4. **Package Name**: `com.Lemala.ComeBack` (как в google-services.json)

#### Шаг 4: Добавьте SubscriptionManager в сцену
1. Создайте пустой GameObject
2. Добавьте компонент `SubscriptionManager`
3. Убедитесь, что он активен

### 3. Тестирование
Запустите игру в Unity Editor. В консоли должны появиться сообщения:

```
[SubscriptionManager] Инициализация Firebase...
[SubscriptionManager] Firebase успешно инициализирован!
[SubscriptionManager] Firebase App инициализирован: __FIRAPP_DEFAULT
[SubscriptionManager] ✅ Используем явный Database URL
[SubscriptionManager] ✅ Настройки подписки загружены успешно
[ARPaymentController] ✅ Значения ПРИНУДИТЕЛЬНО обновлены: Новая цена: 5000 UZS
```

Если видите предупреждение:
```
[SubscriptionManager] ⚠️ Не удалось использовать явный URL, используем DefaultInstance
```
Это нормально - система будет работать с fallback методом.

### 4. Если проблема не решена
1. **Очистите кэш**:
   - Закройте Unity
   - Удалите папки `Library` и `Temp`
   - Откройте Unity заново

2. **Переимпортируйте google-services.json**:
   - Удалите файл из Assets
   - Перетащите обратно

3. **Проверьте интернет-соединение**

## 🎯 Ожидаемый результат
После исправления Unity будет:
- Успешно подключаться к Firebase
- Загружать настройки подписки (5000 UZS, 30 минут)
- Применять их в ARPaymentController
- Показывать правильную цену при оплате

## 📋 Логи для проверки
Правильные логи в Unity Console:
```
💰 Подписка на AR: 5000 сум на 30 минут
[ARPaymentController] 🚀 Создание платежа 5000 сум...
```

Если видите `10000 сум` или `15 минут` - значит Firebase настройки еще не загрузились.
