# 🎮 Настройка Unity для работы с новой системой подписки

## ✅ Что было изменено

### 1. **Размер видео файлов**
- **Было**: до 100 МБ
- **Стало**: до 5 МБ
- ✅ Обновлено в Django админке

### 2. **Система подписки**
- ✅ Добавлен **SubscriptionManager.cs** - получает настройки из Firebase
- ✅ Обновлен **ARPaymentController.cs** - использует настройки из SubscriptionManager
- ✅ **FreedomPayManager.cs** остается без изменений

### 3. **Firebase структура**
- ✅ Добавлен путь `/subscription_settings` с настройками:
  ```json
  {
    "price": 5000.0,
    "duration_minutes": 30,
    "currency": "UZS",
    "is_active": true
  }
  ```

## 🔧 Настройка Unity сцены

### Шаг 1: Добавить SubscriptionManager
1. **Создайте пустой GameObject** в сцене
2. **Назовите его**: `SubscriptionManager`
3. **Добавьте компонент**: `SubscriptionManager.cs`
4. **Настройте в Inspector**:
   - ✅ Debug Mode: `true` (для отладки)
   - ✅ Subscription Price: `5000` (будет обновлено из Firebase)
   - ✅ Subscription Duration Minutes: `30` (будет обновлено из Firebase)

### Шаг 2: Проверить ARPaymentController
1. **Найдите GameObject** с компонентом `ARPaymentController`
2. **Проверьте настройки** в Inspector:
   - ✅ Subscription Price: `5000` (будет обновлено автоматически)
   - ✅ Subscription Duration Minutes: `30` (будет обновлено автоматически)

### Шаг 3: Порядок загрузки
**ВАЖНО**: `SubscriptionManager` должен загружаться **РАНЬШЕ** чем `ARPaymentController`

**Способ 1 - Script Execution Order:**
1. `Edit → Project Settings → Script Execution Order`
2. Добавьте `SubscriptionManager` с приоритетом `-100`
3. Добавьте `ARPaymentController` с приоритетом `0`

**Способ 2 - Hierarchy Order:**
1. Поместите `SubscriptionManager` **ВЫШЕ** в иерархии сцены
2. Поместите GameObject с `ARPaymentController` **НИЖЕ**

## 🔥 Как работает система

### 1. **При запуске приложения:**
```
SubscriptionManager → Firebase → Получает настройки → События
                ↓
ARPaymentController → Подписывается на события → Обновляет цены
                ↓
FreedomPayManager → Использует обновленные цены
```

### 2. **Логи для отладки:**
```
[SubscriptionManager] Loading subscription settings from Firebase...
[SubscriptionManager] Settings loaded:
  Price: 5000 UZS
  Duration: 30 minutes
  Active: True

[ARPaymentController] 🔧 Загрузка настроек подписки...
[ARPaymentController] ✅ Настройки загружены из SubscriptionManager:
  Цена: 5000 UZS
  Длительность: 30 минут
```

### 3. **Обновление цен из Django:**
1. **Админ меняет цену** в Django админке: `http://127.0.0.1:8000/subscription/settings/`
2. **Django отправляет** в Firebase: `/subscription_settings`
3. **Unity получает** обновления **мгновенно** при следующем запуске
4. **FreedomPay использует** новую цену автоматически

## 🚀 Тестирование

### 1. **Проверка настроек:**
1. Запустите Unity приложение
2. Проверьте логи на наличие сообщений от `SubscriptionManager`
3. Убедитесь что цена и время обновились

### 2. **Проверка платежа:**
1. Нажмите кнопку оплаты в AR сцене
2. Проверьте что цена соответствует настройкам из Django
3. Проверьте что время подписки соответствует настройкам

### 3. **Проверка изменения настроек:**
1. Измените цену в Django админке
2. Перезапустите Unity приложение
3. Убедитесь что новая цена применилась

## ❌ Возможные проблемы

### 1. **SubscriptionManager не найден:**
```
[ARPaymentController] ⚠️ SubscriptionManager не найден, используем значения по умолчанию
```
**Решение:** Убедитесь что GameObject с SubscriptionManager создан и активен

### 2. **Firebase не подключен:**
```
[SubscriptionManager] Failed to load subscription settings: Firebase not initialized
```
**Решение:** Проверьте настройки Firebase в Unity

### 3. **Старые цены:**
```
[ARPaymentController] 📋 Значения по умолчанию: 200 сум, 15 минут
```
**Решение:** Проверьте что SubscriptionManager загружается раньше ARPaymentController

## 🎯 Финальная проверка

✅ **SubscriptionManager** добавлен в сцену  
✅ **Script Execution Order** настроен  
✅ **Firebase** подключен и работает  
✅ **Django админка** показывает правильные настройки  
✅ **Unity логи** показывают загрузку настроек  
✅ **Платежи** используют правильную цену  

**Система готова к работе!** 🚀
